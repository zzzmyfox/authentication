syntax = "proto3";

package pb;
option go_package = "authentication/api/pb";

message Request {
  string accessToken = 1;
}

message Response {
  int32  code = 1;
  string msg = 2;
  string body = 3;
}

service Authentication {
  rpc Auth(Request) returns (Response);
}
